package algo

import "gomoku/pkg/playboard"

func MinMaxAlgo(playBoard string, machinePlayer playboard.Player, humanPlayer playboard.Player) *playboard.Pos {
	pos := &playboard.Pos{X: 1, Y: 1}
	return pos
}
